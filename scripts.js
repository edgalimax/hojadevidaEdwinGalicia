document.addEventListener('DOMContentLoaded', () => {
    const imageUrl = 'images/foto.jpg';  // Ruta relativa a la imagen en tu proyecto
    const profileImg = document.getElementById('profileImg');
    
    profileImg.src = imageUrl;
});
